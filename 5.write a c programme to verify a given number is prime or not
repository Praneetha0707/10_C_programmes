#include<stdio.h>
void main()
{
int n,i,rem;
printf("enter the number(n>1):");
scanf("%d",&n);
for(i=2;i<n&&rem>0;i++)
rem=n%i;
  if(rem==0)
  printf("n=%d is not a prime",n);
  else
  printf("n=%d is a prime",n); 
}
